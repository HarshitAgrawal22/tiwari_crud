<!DOCTYPE html>
<html lang="en">
    <head>
        <link rel="stylesheet" href="/styles.css">
        <title>Edit Album</title>
    </head>
    <body>
        <h1>Edit <%= album.musicTitle %></h1>
        <form action="/music-album/<%= album._id %>?_method=PUT" method="POST">
            <input type="text" name="artist" value="<%= album.artist %>"
                required>
            <input type="text" name="genre" value="<%= album.genre %>" required>
            <input type="number" name="releaseYear"
                value="<%= album.releaseYear %>" required>
            <button type="submit">Update Album</button>
        </form>
    </body>
</html>
